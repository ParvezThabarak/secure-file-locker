# Environment Variables for S3 File Manager Backend

# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-change-this-in-production

# Database Configuration (MySQL)
MYSQL_HOST=your-mysql-host
MYSQL_USER=your-mysql-username
MYSQL_PASSWORD=your-mysql-password
MYSQL_DB=secure_file_storage

# Encryption key for AWS credentials (generate a new one for production)
# Use: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AWS_CREDENTIALS_ENCRYPTION_KEY=your-encryption-key-here

# CORS Configuration (comma-separated list of allowed origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-frontend-domain.com

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/app.log